# Invoice preview handoff

The Invoice catalog keeps its own 12 titles and descriptions, but intentionally reuses real documents from the Templates preview library. No separate generated invoice artwork is required.

The image files in this folder are delivery copies named after the Invoice cards. Use [`manifest.csv`](manifest.csv) for the exact source Template, runtime path, and card copy.
